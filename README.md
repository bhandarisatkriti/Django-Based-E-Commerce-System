djangoe
dd

# github commands
git branch -M main

git add .
git commit -m "initial commit"
git push origin main
git status
git version
ssh-keygen
git checkout -b product
git branch -M feat/product
git branch
git log [q for exist]

git add .
git commit -m "mess..."
git push -u origin feat/product



